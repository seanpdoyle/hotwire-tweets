<svg class="h-4 w-4 text-white" viewBox="0 0 16 16">
  <title><%= user ? user.username : "Guest" %></title>

  <text text-anchor="middle" x="50%" y="50%" dy="0.35em" fill="currentColor" font-family="Arial">
    <%= user ? user.username.first : "?" %>
  </text>
</svg>
