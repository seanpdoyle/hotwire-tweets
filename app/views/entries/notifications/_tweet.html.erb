<%= link_to tweet_path(tweet.parent, anchor: dom_id(tweet)), class: "flex text-indigo-500 inline-flex items-center md:mb-2 lg:mb-0 outline-none focus:ring" do %>
  <span class="border rounded-full bg-blue-500 p-1 mr-1">
    <%= render partial: "users/avatar", object: tweet.creator, as: :user %>
  </span>

  <%= tweet.creator.username %> replied to one of your tweets.
<% end %>
